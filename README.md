# Интерактивная визуализация поля облученности

Интерактивное приложение для расчета и визуализации распределения облученности от прямоугольного источника света на прямоугольном приемнике. Приложение использует оптимизированные вычисления с Numba JIT компиляцией и предоставляет интуитивный графический интерфейс для исследования параметров системы.

## 📋 Описание

Проект реализует физическую модель расчета облученности на основе:
- **Закона обратных квадратов**: облученность обратно пропорциональна квадрату расстояния
- **Косинусного распределения**: учет угла падения излучения (закон Ламбера)
- **Векторизованных вычислений**: оптимизация через NumPy и Numba

Приложение позволяет:
- Интерактивно изменять параметры системы (мощность, размеры, позиции)
- Визуализировать поле облученности в реальном времени
- Использовать различные режимы нормализации данных
- Анализировать влияние параметров на распределение облученности

## 🚀 Установка

### Требования

- Python 3.8 или выше
- NumPy
- Matplotlib
- Numba

### Установка зависимостей

```bash
pip install numpy matplotlib numba
```

Или через requirements.txt:

```bash
pip install -r requirements.txt
```

## 📦 Структура проекта

```
Incapsulation/
├── incupsulation.py          # Основной файл приложения
├── interactive_app.py         # Альтернативная версия (без Numba)
├── main.py                   # Исходная версия с async/await
├── main2.py                  # Векторизованная версия
└── README.md                 # Документация
```

## 🎯 Использование

### Запуск приложения

```bash
python incupsulation.py
```

### Параметры управления

#### Основные параметры (слайдеры):

1. **Мощность (Вт)** - 1-5000 Вт
   - Управляет общей мощностью источника излучения

2. **Позиция X (м)** - 0-200 м
   - Горизонтальная позиция центра источника

3. **Позиция Y (м)** - 0-200 м
   - Вертикальная позиция центра источника

4. **Расстояние Z (м)** - 200-1000 м
   - Расстояние между плоскостями источника и приемника

5. **Длина источника (м)** - 0.1-20 м
   - Размер источника по оси X

6. **Высота источника (м)** - 0.1-20 м
   - Размер источника по оси Y

7. **Длина приемника (м)** - 10-500 м
   - Размер приемника по оси X

8. **Высота приемника (м)** - 10-500 м
   - Размер приемника по оси Y

9. **Точность** - 20-80 точек
   - Количество точек разбиения сетки (влияет на скорость расчета)

#### Дополнительные элементы управления:

- **Радио-кнопки нормализации**: выбор типа нормализации данных
  - `linear` - линейная нормализация
  - `log` - логарифмическая шкала (для больших диапазонов)
  - `power` - степенная нормализация с gamma correction

- **Слайдер Gamma**: параметр для power normalization (0.1-2.0)

- **Кнопка "Сброс"**: возврат всех параметров к начальным значениям

- **Кнопка "Быстрый режим"**: установка оптимальной точности для быстрого отклика

## 📊 Интерфейс

Приложение состоит из двух панелей:

### Левая панель: Схема расположения
- Визуализация расположения источника (красный) и приемника (синий)
- Отображение центров объектов
- Линия соединения центров
- Детальная информация о параметрах системы и результатах расчета

### Правая панель: Поле облученности
- Цветовая карта распределения облученности
- Контурные линии (для точности ≤ 50)
- Информация о минимальной и максимальной облученности
- Координаты точки максимальной облученности
- Индикатор типа нормализации

## 🔬 Физическая модель

### Основные формулы

**Облученность в точке приемника:**

\[
E = \frac{I_i \cdot \cos^2(\alpha)}{r^2}
\]

где:
- \(E\) - облученность (Вт/м²)
- \(I_i\) - интенсивность излучения на точку источника (Вт/ср)
- \(\alpha\) - угол между нормалью к приемнику и направлением на источник
- \(r\) - расстояние от точки источника до точки приемника

**Расстояние в 3D:**

\[
r = \sqrt{(x_s - x_r)^2 + (y_s - y_r)^2 + L_{sr}^2}
\]

**Косинус угла:**

\[
\cos(\alpha) = \frac{L_{sr}}{r}
\]

### Особенности реализации

1. **Адаптивное разбиение источника**: количество точек источника зависит от размера и точности расчета
2. **Поправка для малых расстояний**: эмпирическая поправка при расстояниях, сравнимых с размерами объектов
3. **Оптимизация через Numba**: параллельное выполнение вычислений с JIT компиляцией

## ⚡ Производительность

### Оптимизации

- **Numba JIT компиляция**: ускорение в 10-100 раз по сравнению с чистым Python
- **Параллельные вычисления**: использование всех доступных ядер CPU
- **Векторизация**: использование NumPy для операций с массивами

### Рекомендации по точности

- **20-30 точек**: быстрый отклик (< 0.1 сек), для предварительного анализа
- **30-50 точек**: оптимальный баланс скорости и точности (0.1-0.5 сек)
- **50-80 точек**: высокая точность, более медленный расчет (> 0.5 сек)

## 🛠️ Технические детали

### Используемые библиотеки

- **NumPy**: численные вычисления и работа с массивами
- **Matplotlib**: визуализация и интерактивные виджеты
- **Numba**: JIT компиляция для ускорения расчетов

### Архитектура

- **Модульная структура**: разделение расчетной части и интерфейса
- **Оптимизированная функция**: использование Numba decorators для компиляции
- **Интерактивные виджеты**: слайдеры и кнопки для управления параметрами

## 📝 Примеры использования

### Базовое использование

```python
from incupsulation import InteractiveIrradianceApp
import matplotlib.pyplot as plt

app = InteractiveIrradianceApp()
plt.show()
```

### Изменение параметров программно

```python
app = InteractiveIrradianceApp()

# Изменение параметров
app.p = 1000  # Мощность 1000 Вт
app.R = 300   # Расстояние 300 м
app.accuracy = 40  # Точность 40x40

# Обновление визуализации
app.update()
plt.show()
```

## 🐛 Устранение неполадок

### Проблема: Медленный расчет

**Решение**: 
- Уменьшите точность до 20-30 точек
- Используйте кнопку "Быстрый режим"
- Проверьте, что Numba установлена корректно

### Проблема: Ошибка при импорте Numba

**Решение**:
```bash
pip install --upgrade numba
```

### Проблема: График не отображается

**Решение**:
- Убедитесь, что используете интерактивный backend matplotlib
- Попробуйте установить: `pip install PyQt5` или `pip install tk`

## 📚 Литература

- Закон обратных квадратов для электромагнитного излучения
- Закон Ламбера для косинусного распределения излучения
- Методы численного моделирования оптических систем

## 📄 Лицензия

Проект создан в образовательных целях.

## 👤 Автор

Разработано для расчета и визуализации полей облученности в оптических системах.

## 🔄 Версии

- **v1.0** - Базовая версия с async/await
- **v2.0** - Векторизованная версия
- **v3.0** - Оптимизированная версия с Numba (текущая)

---

**Примечание**: Для достижения наилучшей производительности рекомендуется использовать версию с Numba (`incupsulation.py`). Для систем без Numba можно использовать `interactive_app.py`.

